<form method="post" action="{{ path('app_job_tracking_delete', {'id': job_tracking.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_tracking.id) }}">
        <button class="btn">
            Delete
        </button>
    </form>
    