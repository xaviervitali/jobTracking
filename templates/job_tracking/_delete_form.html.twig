<form method="post" action="{{ path('app_job_tracking_delete', {'id': job_tracking.id}) }}" 'data-turbo' = "false"  onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_tracking.id) }}">
        <button class="btn">
            Delete
        </button>
    </form>
    