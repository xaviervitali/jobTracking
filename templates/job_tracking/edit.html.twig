{% set notEditing =  false %}

<h3>
    Modification
</h3>

{{ include('job_tracking/_form.html.twig', {'button_label': 'Modifier'}) }}

{# <a class='btn btn-info' href="{{ path('app_job_tracking', {id: id}) }}">
    Revenir à la candidature
    </a>
    
{{ include('job_tracking/_delete_form.html.twig') }} #}
