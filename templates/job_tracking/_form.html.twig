{{ form_start(formJobTracking, {'attr' : {'data-turbo':"false" } }
) }}
{{ form_widget(formJobTracking.action) }}
{{ form_widget(formJobTracking.createdAt  ) }}
<div class="modal-footer">
    <div class="d-flex justify-content-end gap-1">
        <button class="btn btn-success">
            {{ button_label|default('Save') }}
        </button>
        <button class="btn btn-danger delete" onclick="handleDeleteJobTracking()">
            Supprimer
        </button>
    </div>
</div>
{{ form_end(formJobTracking) }}
