<div class=" card m-3 border rounded bg-white  col-xxl-2 col-xl-3 col-lg-4 col-md-5 col-11">
    <div class="card-body">
        <h5 class="card-title">
            {{ (job.recruiter | default('job_recruiter') |title  ) }}
        </h5>
        <h6 class="card-subtitle mb-2 text-body-secondary">
            {{ job.title  | default('job_title') |title }}
        </h6>
        <p class="card-text">
            {{ job.action_name  | default('job_action_name' ) }}
       
            {{ job.delai  | default('job_delai') | formatDelay }}
        
        </p>
        <p>
            {{ job.note_count | default('job_note_count') }}
            {{   job.note_count | default('job_note_count') |plural('postit', 'postits')}}
        </p>
        <p class="text-end">
        <a href="{{ path('app_job_tracking',  {'id':job.id | default('job_id')}) }}" >
            Visualiser
        </a>
    </p>

    </div>
</div>
